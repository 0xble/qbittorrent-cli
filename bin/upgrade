#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
go build -o ~/.local/bin/qbt ./cmd/qbt
echo "installed qbt from $(git describe --tags --always 2>/dev/null || git rev-parse --short HEAD)"
"$HOME/.local/bin/qbt" version
